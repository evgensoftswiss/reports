# Weekly Planner Manager 0.1.3

Использует тот же `../config.ini`, что Weekly Planner, секцию `[github]`.

Кнопка «Загрузить» скачивает все `Planning_<period>_<user>.json` из папки периода GitHub в локальную `data/<period>/`.
